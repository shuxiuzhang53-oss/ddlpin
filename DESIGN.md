# 任务中枢 + 桌面便签(工作代号:待定)

> 定位:**在你已经待着的地方顺口记下,在你躲不开的地方一直显示,在快来不及时把你拽回来。**
> 面向人群:经常忘事、和 AI 交互极多、懒得主动开备忘录的人。

## 实现状态(MVP 已跑通,2026-08-11)

**已实现并验证:**
- ✅ daemon:SQLite(node 内置,零依赖)+ HTTP API + 提醒调度器 + board.md 镜像;launchd 常驻自启
- ✅ MCP server(零依赖手写 stdio):`add_task/list_tasks/update_task/complete_task/snooze_task/drop_task` + `due-soon` 资源;已在 Claude Code user 级注册
- ✅ Electron 便签:置顶/全空间可见/半透明、实时倒计时、紧急度配色、勾选完成、贪睡、输入框加任务、迷你胸章形态、SSE 实时刷新;launchd 常驻
- ✅ 记录时按 cwd **自动打项目标签**
- ✅ 提醒阶梯(还有1天/当天早9点/还有1小时/10分/到点)+ 逾期每30分死缠 + 每日晨报;macOS 系统通知 + ntfy 手机推送(需配 topic)
- ✅ 可信回显:MCP 返回归一化任务(绝对截止时间 + 倒计时)供 AI 复述

**待办(按设计 §8.5 分级):**
- ⏳ Codex / mirasim 入口:同一个 MCP,加配置即可(见 README)
- ⏳ 每日自动滚动"改期还是丢掉"交互(现仅逾期死缠)、无法解析进 Inbox
- ⏳ 被动侦测承诺、"放不下"预警、焦点感知、一键跳回上下文(V1)
- ⏳ 手机通知动作按钮(需 daemon 可被手机访问)、任务反向派给 agent(V2)

---

## 0. 设计原则(全部服务于"健忘 + AI 重度 + 懒"这三点)

1. **在原地捕获(Capture in place)** —— 你在哪个 AI 里说话,就在哪里能记。不需要切窗口、不需要打开任何 App。
2. **躲不开的呈现(Ambient display)** —— 桌面常驻便签是第一道防线,不靠"你想起来去看"。
3. **升级式提醒(Escalating reminders)** —— 越临近越吵,从"变个颜色"一路升到"全屏挡住你 + 手机推送"。
4. **可信才有用(Trust loop)** —— 每次记录都回显解析结果(把"周五18点"变成绝对时间念给你听),记错能一句话改。捕获不可信,人就会退回到"靠脑子记"。
5. **零维护(Zero housekeeping)** —— AI 自动分类、排序、归档。你只负责"说"和"做",不负责"整理看板"。

---

## 1. 架构总览:中枢 daemon + 多适配器

关键决策:**不做成某个 AI 的插件,而是做一个独立的本地中枢服务,所有 AI/入口都只是它的"写入适配器"。** 这样加一个新入口(Codex、mirasim、手机……)只是加一个薄适配器,核心不动。

```
        输入侧(Capture,你在哪说都行)                     中枢(唯一真相源)              呈现侧(躲不开)
 ┌──────────────────────────────────────┐        ┌────────────────────────┐      ┌───────────────────────┐
 │  Claude Code  ──(MCP stdio)──┐        │        │   Task Daemon          │      │  桌面便签窗口          │
 │  Codex CLI    ──(MCP stdio)──┤        │        │  ┌──────────────────┐  │      │  · 常驻置顶/全空间可见 │
 │  mirasim 窗口 ──(HTTP/MCP)───┼───────▶│  HTTP  │  │ SQLite(真相源)  │  │─────▶│  · 倒计时 + 颜色分级   │
 │  全局热键快记 ──(local)──────┤        │  API   │  │ + board.md 镜像  │  │  WS  │  · 勾选/贪睡/改期      │
 │  菜单栏输入框 ──────────────┘        │        │  └──────────────────┘  │      └───────────────────────┘
 │  手机(可选)  ──(HTTP over LAN)──────┘        │  ┌──────────────────┐  │      ┌───────────────────────┐
 └──────────────────────────────────────┘        │  │ 提醒调度器        │──┼─────▶│  升级式提醒            │
                                                  │  │(每30s扫到期)    │  │      │  系统通知→全屏浮层     │
                                                  │  └──────────────────┘  │      │  →声音→手机推送(Bark) │
                                                  └────────────────────────┘      └───────────────────────┘
```

- **唯一真相源 = 本地 daemon + SQLite**。所有入口都往它写,便签只是它的一个视图。
- **MCP server = 薄转发层**:CC / Codex 各自 spawn 一个 stdio MCP 进程,它只是把工具调用转成对 daemon 的 HTTP 请求。CC 和 Codex 复用同一个 MCP 二进制,配置文件不同而已。
- **mirasim / 手机 / 浏览器插件**:直接调 daemon 的 HTTP API(或走 MCP over HTTP/SSE)。

### 为什么用 SQLite 而不是纯 Markdown 做核心
提醒调度器每 30 秒要查"未来 1 分钟内到期的提醒",多个入口会并发写,还要按 urgency 排序——这些用 SQLite(WAL 模式)是一行 SQL,用 Markdown 每次全文解析 + 抢文件锁会很痛。
**折中:SQLite 做真相源,daemon 每次变更自动导出一份 `board.md` 人类可读镜像**(可 git、可手翻、可手改后回灌)。两全其美。

---

## 2. 输入侧:多入口捕获

### 2.1 三个 AI 入口 —— 全部走 MCP(已评估 mirasim 源码后确认)
| 入口 | 接入方式 | 配置位置 | 工作量 |
|---|---|---|---|
| Claude Code(直接) | MCP (stdio) | `~/.claude` 的 MCP 配置 | 配一段 |
| Codex CLI(直接) | MCP (stdio) | `~/.codex/config.toml` 的 `mcp_servers` | 配一段 |
| **mirasim 里的会话** | **同一个 MCP** | 注册为 mirasim 全局 MCP 扩展 | **零新代码** |

**关键认知:mirasim 是 agent 编排器,它自己不带 agent,"一个窗口"其实就是它启动的一个 Claude Code / Codex 会话**(ARCHITECTURE.md §5、§4 的 `session` tile)。所以 mirasim 的输入口和直接用 CC/Codex 是同一类 MCP 消费者,**不需要给 mirasim 单独写 HTTP 适配器**。两条佐证:
- mirasim 明确支持 MCP 扩展:"MCP servers and skills can be shared globally or scoped to a workspace"(§5.3),注册一次 → 每个会话自动带上 `add_task`。
- mirasim 的"黄金法则":`runHijacked` 启动 agent 时只改模型 base URL,`stdio:'inherit'`,**不碰 agent 的 MCP 配置**,所以为直接版 CC/Codex 配的 MCP,mirasim 启动它们时也会继承。

> **同一套 MCP 工具,三处复用。** 新增第 4、第 5 个 AI 入口 = 复制一段配置。
> **mirasim 的 HTTP 只在一种可选场景才需要**:将来想在 mirasim 自己的 UI 里做一个"第一方任务看板面板"(显示/编辑,而非通过 agent 记录),那个面板才去读 daemon 的 HTTP API。捕获永远走 MCP。

### 2.2 MCP 工具集(给 AI 用)
- `add_task(title, due?, priority?, list?, notes?, reminders?)` —— 记一条。
- `list_tasks(filter?)` —— 查(今天/逾期/某项目)。
- `update_task(id, ...)` —— 改期、改优先级、改标题。
- `complete_task(id)` / `snooze(id, until)` / `delete_task(id)`。
- **资源(MCP resource)`due-soon`** —— 暴露"今天到期 + 逾期"列表。CC/Codex 每次开新会话可读它,于是 **AI 会在对话里主动提醒你**("顺便说一句,你有 2 个任务今天到期")。这是给健忘的人埋的钩子。

### 2.3 自然语言解析:直接甩给 AI
- 你说"截止周五18点",**由 CC/Codex 自己**(它们本来就是 LLM)结合当前日期解析成绝对 ISO 时间再调用 `add_task`。工具 schema 强制要求传绝对时间。**我们一行 NLP 都不用写。**
- 非 AI 入口(热键快记)用本地轻量解析(chrono 之类)兜底,解析不了就丢进"待整理 Inbox"。

### 2.4 可信回显(防记错)
`add_task` 返回归一化后的任务(标题 / **绝对到期时间** / 优先级 / 所属清单 / 已排的提醒点),让 AI 复述给你 → 你当场就能发现"它把周五理解成上周五了"。

### 2.5 非 AI 场景的快记(懒人补充)
- **全局热键**(如 ⌥Space)弹一个极简输入框,一行文字→一条任务,不打断当前工作。
- **菜单栏图标**常驻,点开就是输入框 + 今日清单。
- 可选:浏览器插件"选中文字→加为任务"、邮件转任务、手机快捷指令。

---

## 3. 提醒引擎(为"健忘"量身定制,这是产品的命根子)

大多数待办 App 只会"到点弹一下通知,你正好没看手机就漏了"。对健忘的人,单点提醒 = 没提醒。所以做**多层 + 升级 + 死缠烂打**。

### 3.1 自动提醒阶梯(按剩余时间自动生成,你不用手设)
- 截止在数天后:**前1天 → 当天早上 → 前1小时 → 到点 → 逾期**。
- 截止在数小时内:**前1小时 → 前10分钟 → 到点**。
- 无截止(someday):不提醒,只在便签低优先级区待着。
- 你也可以一句话覆盖:"这个提前两小时提醒我"。

### 3.2 升级式通道(越临近越有侵略性)
1. **环境级**:便签里该任务变色 + 自动排到最上(始终在,不依赖你去看)。
2. **系统通知**:macOS 通知中心。
3. **自绘全屏浮层**:一个 App 自己画的、置顶、居中的大字提醒卡,**必须点"知道了/贪睡/改期"才消失**——不受系统"勿扰模式"静音影响,这是对付"通知被折叠/静音"的杀手锏。
4. **声音 + 菜单栏红色角标**。
5. **手机推送(安卓)**:主用 **ntfy**(开源免费、安卓 App、**通知带动作按钮**可直接"完成/贪睡/改期");辅以**飞书机器人**(复用 mirasim 已有 IM 通道,手机不用装新 App,可回复操作)。备选 Pushover(跨平台、$5 一次性)。人离开电脑也能收到并处理。

### 3.3 逾期死缠 & 贪睡
- 逾期后每 N 分钟重复提醒,直到你**勾选完成**或**改期**(不能被无视掉)。
- 一键贪睡:"1 小时后再说"、"明早再说"。

### 3.4 每日晨报
每天早上一条汇总(通知 + 便签置顶):今天到期 X 条、逾期 Y 条。并且当天你一开 CC,AI 也会顺口念(见 2.2 的 `due-soon` 资源)。

---

## 4. 呈现侧:桌面常驻便签

- **技术**:MVP 用 Electron 起手快;长期可换 Tauri(常驻内存占用低,适合一直挂着)。窗口 frameless、`alwaysOnTop`、`visibleOnAllWorkspaces`、半透明、可拖动/缩放、记住位置。
- **内容**:按 urgency 排序的清单,每条 = 标题 + **实时倒计时** + 颜色带(绿/黄/红/逾期闪烁)。
- **交互(极简)**:勾选完成、贪睡、点开看详情/改期。右键更多。
- **多形态**:紧凑条(一行滚动最紧急的)/ 竖排清单 / 分项目多列看板,可切换。
- **原则**:一眼扫到,不需要"打开"任何东西——这就是它存在的全部理由。

---

## 5. 自动整理(AI 驱动的零维护)

- 记录时 AI 自动填:所属项目/标签/优先级(你可不填)。
- **urgency 分数** = f(剩余时间, 优先级),便签和列表据此自动排序。
- 完成的任务自动归档(保留 N 天可撤销)。
- 可选:一个定时 CC/cron 任务定期"AI 整理"——去重、把没截止的老任务拎出来问你、合并重复。

---

## 6. 可靠性(健忘的人最怕"以为记了其实没记")

- **daemon 开机自启**(launchd),菜单栏有健康指示灯;挂了要显眼。
- daemon 没跑时,MCP 工具**大声报错**(而不是假装成功),AI 会告诉你"没记上"。
- 每次记录有绝对时间回显 + 一键撤销。
- SQLite WAL 抗并发;`board.md` 镜像 + 定期备份防数据丢失。

---

## 7. 部署拓扑(已定)

- **mirasim = 同一台 Mac 上的窗口** → 和 CC/便签同机。mirasim 的窗口是它启动的 CC/Codex 会话,**走 MCP 记录**(见 §2.1),不需要 HTTP。daemon 只监听 `127.0.0.1:<port>` 供 MCP 转发层和便签使用,**无需联网、无需鉴权**。
- **手机 = 安卓**,提醒走 **ntfy**(主,通知带动作按钮可直接完成/贪睡/改期)+ **飞书机器人**(辅,复用 mirasim IM,可回复操作)。只有"提醒推送"这一件事走云:daemon 到点向 ntfy topic / 飞书发一个 HTTPS 请求即可,不用自己搭服务器、不暴露本地端口。ntfy 也可自托管。
- 结论:整套系统本地闭环,唯一出网的是"给手机发提醒"。

---

## 8. 落地路线

**MVP(先跑通闭环 + 几个低成本高收益的"方便"项):**
1. daemon:SQLite + HTTP API(add/list/update/complete)+ 提醒调度器 + `board.md` 镜像/备份。
2. MCP server(转发到 HTTP),接上 Claude Code;记录时**从 cwd 自动打项目标签**。
3. Electron 便签窗口:置顶、倒计时、颜色分级、勾选完成。
4. 系统通知 + 逾期死缠 + **可操作通知**(完成/贪睡/改期)。
5. **每日自动滚动**:到期没做完的次日问"改期还是丢掉";无法解析的进 Inbox。
→ 此时:在 CC 里(顺口)说一句 → 自动归到项目 → 便签出现 → 到点可操作提醒 → 没做完不烂在坟场。核心价值已成立。

**V1(补齐多入口 + 提醒力度):**
5. 接 Codex(同一个 MCP,加配置)。
6. 接 mirasim(HTTP)。
7. 全屏浮层提醒 + 每日晨报 + `due-soon` 资源(AI 主动念)。
8. 全局热键快记 + 菜单栏。

**V2(锦上添花):**
9. 手机推送(Bark)、贪睡策略细化、`board.md` 双向同步、AI 定期整理、多形态便签。

---

## 8.5 增强项:让它"真的替健忘懒人兜底"(按 persona 对症)

> 核心洞察:上一版解决"你说了就记、记了就提醒";真正的漏点是 ①很多任务你不会专门去记,只是跟 AI 顺口提;②到点提醒时你正忙/开会/已经来不及。下面按任务生命周期补齐。每条标了建议落地阶段。

### A. 捕获:从"你主动记"→"它主动帮你记"
- **[V1] 被动侦测承诺(最大杠杆)** —— agent 被动听对话,听到带时间的承诺就问一句"要记到看板吗?截止…?",确认后才落库。默认开、可关、**只本地、先确认再落**(隐私红线)。在源头治健忘。实现:MCP 工具描述 + 一条全局指令让 agent 主动 offer;不做流量离线解析。
- **[MVP] 上下文自动归属** —— MCP server 从 cwd/workspace 拿到当前项目,记任务时自动打标签/分组,零手填。
- **[V1] 飞书/IM 转任务** —— 复用 mirasim 已接的 7 个 IM 通道:转发消息或发"记:…"进看板。人在手机飞书里也能记。
- **[V2] 手机语音快记** —— iOS 快捷指令 + Siri"记任务"→ 打 daemon(需 §7 的 LAN/中继)。
- **[V1] 便签即输入** —— 便签窗口顶部一行输入框,直接敲=一条任务(不经过 AI 时的最快路径)。

### B. 提醒:更聪明,不是更吵
- **[V1] "放不下"预警** —— 记录时 AI 顺带估工时(可选字段);剩余时间 < 预估工时就提前预警"塞不下了",而不是到点才发现来不及。
- **[V1] 焦点感知升级** —— 检测"开会/CC 正跑长任务"时先走安静通道,被忽略再升级到全屏浮层。该狠时狠,不该断时不断。
- **[MVP] 可操作通知** —— 通知/浮层/Bark 上直接"完成 / 贪睡 / 改期",不用回电脑。
- **[V1] 智能时间默认 + 学习** —— "明天/下班前"→ 具体时刻(默认 18:00,可配),逐步学你的习惯,减少来回确认。

### C. 执行闭环(本技术栈独门)
- **[V2] 任务反向派给 agent** —— 开发类任务一键甩给 CC/mirasim 让 agent 直接开干。捕获→提醒→**真把活干了**。
- **[V1] 一键跳回上下文** —— 点便签任务 → 打开它来源的 CC 会话/项目/文件,立刻接着干。

### D. 呈现 & 安心兜底
- **[V1] 迷你胶囊形态** —— 缩成小胶囊只显示"下一件事+倒计时",hover 展开。又在眼前又不碍事。
- **[MVP] 每日自动滚动 + 收尾复盘** —— 到期没做完的,次日问"改期还是丢掉",不让看板烂成逾期坟场(**保命:坟场化=用户不再信任=退回用脑子记**)。
- **[V2] 每周回顾** —— 汇总完成/搁置/长期没动的,一次性清理。
- **[MVP] 数据不丢** —— 无法解析的丢进 Inbox 待整理;`board.md` 放进 git/iCloud 目录自动留存 + 定期备份。

## 9. 已定决策
- **技术栈**:daemon 用 Node/TS(MCP SDK 成熟、和 Electron 同栈)。✅
- **核心存储**:SQLite(真相源)+ `board.md` 人类可读镜像。✅
- **部署拓扑**:全部同机 localhost 闭环;唯一出网 = Bark 手机推送。✅(见 §7)
- **mirasim**:同一台 Mac 上的窗口,走 localhost HTTP API 接入。✅
- **手机推送**:安卓 → **ntfy**(已选定,可操作通知:完成/贪睡/改期;ntfy.sh 开箱即用,可自托管)。飞书机器人为可选加强,不进 MVP。✅
- **便签框架**:**跨平台 Mac + Win**,用 Electron(跨平台成本最低);全屏浮层/开机自启做两套平台适配。✅
```

